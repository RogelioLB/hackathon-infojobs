/** @type {import('next').NextConfig} */
const nextConfig = {
    images:{
        remotePatterns:[
            {
                hostname:"multimedia.infojobs.net"
            }
        ]
    }
}

module.exports = nextConfig
